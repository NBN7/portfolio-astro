---
interface Props {
  id: string;
}

const { id } = Astro.props;
---

<section
  id={id}
  class="w-full min-h-screen flex flex-col items-center justify-center"
>
  <div class="sm:w-[400px] w-full flex flex-col gap-3">
    <slot />
  </div>
</section>
